{
  "service_id": "aws_ec2",
  "service_name": "AWS EC2 Instance",
  "version": "2026-04-15",
  "field_order": [
    "application_name",
    "app_id",
    "account_id",
    "environment",
    "region",
    "availability_zone",
    "instance_type",
    "ami",
    "compute_type",
    "disk_size_gb"
  ],
  "fields": {
    "application_name": {
      "type": "select",
      "required": true,
      "readonly": false,
      "source": "redis",
      "options_key": "application_names",
      "depends_on": null,
      "prompt": "Select application name",
      "help": "The application this EC2 instance belongs to. Determines account and permissions.",
      "validator": null
    },
    "app_id": {
      "type": "text",
      "required": true,
      "readonly": true,
      "source": "user_enrichment",
      "options_key": null,
      "depends_on": null,
      "prompt": "App ID (auto-filled)",
      "help": "Application ID from the enterprise app registry. Auto-filled based on application name.",
      "validator": null
    },
    "account_id": {
      "type": "text",
      "required": true,
      "readonly": true,
      "source": "user_enrichment",
      "options_key": null,
      "depends_on": null,
      "prompt": "Account ID (auto-filled)",
      "help": "AWS account number. Auto-filled from application/environment mapping.",
      "validator": null
    },
    "environment": {
      "type": "select",
      "required": true,
      "readonly": false,
      "source": "redis",
      "options_key": "environments",
      "depends_on": null,
      "prompt": "Select environment",
      "help": "Target deployment environment. DEV for development, QA for testing, PROD for production.",
      "validator": null
    },
    "region": {
      "type": "select",
      "required": true,
      "readonly": false,
      "source": "redis",
      "options_key": "regions",
      "depends_on": null,
      "prompt": "Select AWS region",
      "help": "Geographic AWS location where the instance will be deployed. Affects latency and compliance.",
      "validator": null
    },
    "availability_zone": {
      "type": "select",
      "required": true,
      "readonly": false,
      "source": "redis",
      "options_key": "availability_zones_by_region",
      "depends_on": "region",
      "prompt": "Select availability zone",
      "help": "A sub-location within the selected region. Provides fault isolation. Options depend on the selected region.",
      "validator": null
    },
    "instance_type": {
      "type": "select",
      "required": true,
      "readonly": false,
      "source": "redis",
      "options_key": "instance_types",
      "depends_on": null,
      "prompt": "Select instance type",
      "help": "Defines CPU, memory, and network capacity. t3 is burstable, m5 is general purpose, c5 is compute optimized.",
      "validator": null
    },
    "ami": {
      "type": "select",
      "required": true,
      "readonly": false,
      "source": "redis",
      "options_key": "amis_by_region",
      "depends_on": "region",
      "prompt": "Select AMI (machine image)",
      "help": "Amazon Machine Image used to boot the instance. Ubuntu and Amazon Linux are common choices. Options depend on the selected region.",
      "validator": null
    },
    "compute_type": {
      "type": "select",
      "required": true,
      "readonly": false,
      "source": "redis",
      "options_key": "compute_types",
      "depends_on": null,
      "prompt": "Select compute type",
      "help": "Business-facing grouping: general-purpose (balanced), memory-optimized (high RAM), compute-optimized (high CPU).",
      "validator": null
    },
    "disk_size_gb": {
      "type": "number",
      "required": true,
      "readonly": false,
      "source": "user_input",
      "options_key": null,
      "depends_on": null,
      "prompt": "Enter root disk size in GB",
      "help": "Size of the root EBS volume. Minimum 20 GB, maximum 500 GB.",
      "validator": {
        "min": 20,
        "max": 500
      }
    }
  },
  "user_enrichment": {
    "application_name": "KAW-ACC-00N-TSX-JARQA",
    "app_id": "APP000300003369",
    "account_id": "050752608746"
  },
  "default_json": {
    "service_id": "aws_ec2",
    "resource_type": "vm",
    "owner": "jarvis-ai",
    "request_source": "agent_2",
    "provisioning_tool": "terraform"
  }
}
